host: "${host}"
username: "${user}"
password: "${password}"
clientId: ${clientId}
tls:
  useTls: false
opinions:
  web control admin: 1
  web control paco: 0.4
  cli: 0.9
messages:
  - topic: "test/bench/${clientId}"
    content: "${message}"
    interval: 0
    qos: ${qos}

