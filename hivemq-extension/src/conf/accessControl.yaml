permissions:
  - topic: control/view/+
    rules:
      - action: subscribe
  - topic: control/mod/+
    rules:
      - action: publish
        username: admin
  - topic: tmgr/ping
    rules:
      - action: subscribe
        qos: one_two
  - topic: test/#
    rules:
      - clientId: cli
        username: test
        trust: 0.5
      - username: admin
